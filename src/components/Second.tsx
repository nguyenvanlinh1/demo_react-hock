import { Typography } from "@mui/material";

const Second = () => {
  return (
    <div>
      <Typography variant="h3" fontWeight={600}>
        10
      </Typography>
    </div>
  );
};

export default Second;
