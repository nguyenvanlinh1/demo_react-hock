import { Typography } from "@mui/material";

const Minues = () => {
  return (
    <div>
      <Typography variant="h3" fontWeight={600}>
        10 <span>:</span>
      </Typography>
    </div>
  );
};

export default Minues;
